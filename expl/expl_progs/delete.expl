int main()
{
decl
	int temp;
	str fname;
enddecl

begin
	temp = exposcall("Write",-2,"Enter file name");
	temp = exposcall("Read",-1,fname);
	
	temp = exposcall("Delete",fname);
    if (temp == -2) then
        write("File is Open");
        write("Cannot Delete");
    endif;
    if (temp == -1) then
        write("Not a DAT file/");
        write("No permission");
        write("Delete Failed");
    endif;
    if (temp == 0) then
        write("Success");
    endif;
	return 0;
end
}
