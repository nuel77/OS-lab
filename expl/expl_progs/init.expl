int main()
{
decl
	int pid,a,t;
enddecl
begin
    breakpoint;

	pid = exposcall("Fork");
	if(pid == 0) then
		a = exposcall("Exec","odd.xsm");
        if (a == -1) then
            t = exposcall("Write",-2,"No file");
        endif;
	else
		a = exposcall("Exec","even.xsm");
        if (a == -1) then
            t = exposcall("Write",-2,"No file");
        endif;
	endif;
	
    return 0;
end
}